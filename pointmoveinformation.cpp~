#include "pointmoveinformation.h"

PointMoveInformation::PointMoveInformation(QPoint startPos, QPoint endPos, int moveTime, int startTime, int lifeTime):startPos(startPos),endPos(endPos),moveTime(moveTime),startTime(startTime),lifeTime(lifeTime)
{

}

